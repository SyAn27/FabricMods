minecraft {
    accessWidener = file('src/main/resources/expandedstorage.accesswidener')
}

repositories {
    flatDir {
        dirs file('runtime_mods')
    }
    maven {
        name = "JitPack"
        url = "https://jitpack.io"
    }
}

dependencies {
    compile project(path: ":chainmail"), withoutUnwanted

    include project(path: ":chainmail")
    include "io.github.cottonmc:Jankson-Fabric:${jankson_version}"

    modCompileOnly("com.github.Virtuoel:Towelette:${towelette_commit}") {
        exclude(group: "com.github.Virtuoel", module: "Statement")
    }
    modCompileOnly("com.github.Virtuoel:Statement:${statement_commit}")

    // todo: grab off curseforge
    //modRuntime('local:InventorySorter:1.7.0-1.16')
    //modRuntime('local:inventoryprofiles:fabric-1.16.1-0.4.2')
}